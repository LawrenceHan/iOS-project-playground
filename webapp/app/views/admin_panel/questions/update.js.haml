- if @question.errors.empty?
  $('tr#question_#{@question.id}').replaceWith("#{j(render('question', question: @question))}");
  $.fancybox.close();
- else
  alert("#{@question.errors.full_messages.join('\n')}");
